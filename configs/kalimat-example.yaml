# Generated by: ragdiff generate-adapter (MOCK EXAMPLE)
# OpenAPI Spec: https://api.kalimat.dev/openapi.json
# Test Query: "ما هو التوحيد"
# Generated: 2025-10-26
#
# This is an example configuration showing what would be generated
# for the Kalimat Islamic knowledge search API.
#
# Usage:
#   export KALIMAT_API_KEY=your_api_key_here
#   ragdiff query "ما هو التوحيد" --tool kalimat --config configs/kalimat-example.yaml

kalimat:
  adapter: openapi
  api_key_env: KALIMAT_API_KEY
  timeout: 30
  max_retries: 3

  options:
    # API Configuration
    base_url: https://api.kalimat.dev
    endpoint: /v1/search
    method: POST

    # Authentication (API Key in header)
    auth:
      type: api_key
      header: X-API-Key

    # Request Template
    request_body:
      query: "${query}"
      limit: ${top_k}

    # Response Mapping (JMESPath)
    # AI-generated from example response structure
    response_mapping:
      results_array: "data.results"

      fields:
        # Required fields
        id: "id"
        text: "content.text"
        score: "relevance_score"
        source: "source.name"

        # Optional metadata (JMESPath object construction)
        metadata: "{author: metadata.author_latin, type: source.type, category: metadata.category, chapter: source.chapter}"

    # Optional: Add custom headers if needed
    # request_headers:
    #   User-Agent: "RAGDiff/1.0"
